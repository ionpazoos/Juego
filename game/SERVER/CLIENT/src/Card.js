export default class HighScore{

    constructor (xPos, yPos, id, name, score){

        this.xPos           = xPos;             // Posicion inicial en X
        this.yPos           = yPos;             // Posicion inicial en Y
        this.isbn           = isbn;
        this.id             = id;
        this.name           = name;
        this.score          = score;
    }
}